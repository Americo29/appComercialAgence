import 'package:flutter/material.dart';



class GraphBarScreen extends StatelessWidget {
  const GraphBarScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('GraphBar Screen'),
    );
  }
}